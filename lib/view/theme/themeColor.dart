import 'package:flutter/material.dart';

// for light theme color scheme
  final lightBgColor = Color(0xffD1D9E6);
  final lightTextColor = Color(0xff000000);
  final lightDivColor = Color(0xffffffff);
  final lightLableColor = Color(0xffffffff);

// common color
  final darkColor = Color(0xff000000);
  final lightColor = Color(0xffffffff);
  final buttonColor = Color(0xFF0A7DF3);

// for dark theme color scheme
  final darkBgColor = Color(0xff000000);
  final darkTextColor = Color(0xffffffff);
  final darkDivColor = Color(0xff373737);
  final darkLableColor = Color(0xff8C8C8C);
